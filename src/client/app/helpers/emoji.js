// note to self:
// https://github.com/iamcal/js-emoji
// https://gist.github.com/mwunsch/4710561
// ...

/**
 * Convert GitHub-like Emoji shortcodes into actual unicode Emoji characters.
 * @param String text
 * @return String
 */
app.helpers.encodeEmoji = function(text) {
	return text;
};

/**
 * Convert unicode Emoji characters into images
 */
app.helpers.decodeEmoji = function(text) {
	return text;
};